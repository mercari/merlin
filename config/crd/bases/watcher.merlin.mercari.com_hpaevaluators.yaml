
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.4
  creationTimestamp: null
  name: hpaevaluators.watcher.merlin.mercari.com
spec:
  group: watcher.merlin.mercari.com
  names:
    kind: HPAEvaluator
    listKind: HPAEvaluatorList
    plural: hpaevaluators
    singular: hpaevaluator
  scope: Cluster
  validation:
    openAPIV3Schema:
      description: ' HPAEvaluator is the Schema for the hpaevaluators API'
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            ignoreNamespaces:
              description: IgnoreNamespaces is the list of namespaces (string) to
                ignore
              items:
                type: string
              type: array
            rules:
              description: Rules are the rules to check for the evaluator
              properties:
                invalidMetrics:
                  properties:
                    enabled:
                      type: boolean
                  type: object
                reachedMaxReplica:
                  properties:
                    enabled:
                      type: boolean
                  type: object
              type: object
          type: object
        status:
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
