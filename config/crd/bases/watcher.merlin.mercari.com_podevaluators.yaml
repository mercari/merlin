
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.4
  creationTimestamp: null
  name: podevaluators.watcher.merlin.mercari.com
spec:
  group: watcher.merlin.mercari.com
  names:
    kind: PodEvaluator
    listKind: PodEvaluatorList
    plural: podevaluators
    singular: podevaluator
  scope: Cluster
  validation:
    openAPIV3Schema:
      description: PodEvaluator is the Schema for the podevaluators API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: PodEvaluatorSpec defines the desired state of PodEvaluator
          properties:
            ignoreNamespaces:
              description: IgnoreNamespaces is the list of namespaces (string) to
                ignore
              items:
                type: string
              type: array
            rules:
              description: Rules is the list of checks to perform for the namespace
              properties:
                belongsToService:
                  properties:
                    enabled:
                      type: boolean
                  type: object
                managedByPDB:
                  properties:
                    enabled:
                      type: boolean
                  type: object
                managedByReplicaset:
                  properties:
                    enabled:
                      type: boolean
                  type: object
                requiredAnnotations:
                  items:
                    properties:
                      enabled:
                        type: boolean
                      key:
                        type: string
                      value:
                        type: string
                    type: object
                  type: array
                restarts:
                  properties:
                    enabled:
                      type: boolean
                    threshold:
                      format: int32
                      type: integer
                  type: object
              type: object
          type: object
        status:
          description: PodEvaluatorStatus defines the observed state of PodEvaluator
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
