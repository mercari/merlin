
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.4
  creationTimestamp: null
  name: deploymentevaluators.watcher.merlin.mercari.com
spec:
  group: watcher.merlin.mercari.com
  names:
    kind: DeploymentEvaluator
    listKind: DeploymentEvaluatorList
    plural: deploymentevaluators
    singular: deploymentevaluator
  scope: Cluster
  validation:
    openAPIV3Schema:
      description: DeploymentEvaluator is the Schema for the deploymentevaluators
        API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: DeploymentEvaluatorSpec defines the desired state of DeploymentEvaluator
          properties:
            ignoreNamespaces:
              description: IgnoreNamespaces is the list of namespaces (string) to
                ignore
              items:
                type: string
              type: array
            rules:
              description: Rules are the rules to check for the evaluator
              properties:
                canary:
                  properties:
                    description:
                      type: string
                    enabled:
                      type: boolean
                  type: object
                replica:
                  properties:
                    description:
                      type: string
                    enabled:
                      type: boolean
                    min:
                      format: int32
                      type: integer
                  type: object
              type: object
          type: object
        status:
          description: DeploymentEvaluatorStatus defines the observed state of DeploymentEvaluator
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
