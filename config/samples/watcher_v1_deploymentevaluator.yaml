apiVersion: watcher.merlin.mercari.com/v1
kind: DeploymentEvaluator
metadata:
  name: deployment-evaluator
spec:
  ignoreNamespaces:
    - kube-system
    - isto-system
    - test
    - spinnaker
    - cert-manager
    - prometheus
  rules:
    hasCanary:
      enabled: false
    replica:
      min: 2
      enabled: false
