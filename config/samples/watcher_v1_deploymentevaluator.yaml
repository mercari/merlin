apiVersion: watcher.merlin.mercari.com/v1
kind: DeploymentEvaluator
metadata:
  name: deployment-evaluator
spec:
  # Add fields here
  canary:
    enabled: true
  ignoreNamespaces:
    - kube-system
    - isto-system
    - test
    - spinnaker
    - cert-manager
    - prometheus
